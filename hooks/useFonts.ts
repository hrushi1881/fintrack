import { useFonts } from 'expo-font';

export const useCustomFonts = () => {
  // Temporarily disable custom fonts to avoid loading issues
  // Return true to indicate fonts are "loaded" (using system fonts)
  return true;
};
